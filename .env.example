# AgencyFlow — Environment Variables (example)
# Copie para .env e preencha os valores

JWT_SECRET=seu-jwt-secret-aqui
PORT=3001
CORS_ORIGIN=http://localhost:3001
NODE_ENV=development
BRIDGE_TIMEOUT_MS=300000
